/**
 * NÃO TERÁ CÓDIGO EXECUTÁVEL NESSE PROJETO
 */

// *** PROCEDURAL
function processamento (valor1, valor2, valor3){
    /**
     * vamos imaginar que essa seja uma função, nesse método procedural
     * nesse caso, temos valores que compatilham infomações entre escopo global
     * e outras funções por exemplo, essa fução recebe como parâmetro valores que sertão 
     * procesados e poderão ou não ser retornados em forma de cauculos matemáticos, arrays entre outros...
     * */ 
}

// ### ORIENTADO A OBJETOS
object = {
    // dentro dos objetos temos valores ou atributos internos do objetos
    valor1,
    valor2,
    valor3,
    /**
     * e também temos dentro dos objetos comportamento por exemplo
     * quando estamos trabalhando com método procedural básicamente 
     * nós estamos recebendo dados manupulando e devolvendo valores.
     * Quando passamos para OO nós temos os atributos e funções dentro dos objetos, 
     * e dessa forma passamos a acessar os conteúdos do objeto através da palavras que 
     * corresponde ao nome do objeto seguido do . e o nome do atributo ou função que
     * se queira aceesar dentro do objeto, por exemplo: Object.processamento(); ou Object.valor1 e etc...
     * 
     */
    processamento() {

    }
}

object.processamento();// chamando um dado de dentro de um objeto