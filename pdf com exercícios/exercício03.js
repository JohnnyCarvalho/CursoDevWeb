/**
 * 03) Crie uma função que recebe dois parâmetros, 
 * base e expoente, e retorne a base elevada ao expoente.
 * @param {*} base 
 * @param {*} expoente 
 */

function exponeciacao(base, expoente){
    console.log("O resultado é: "+(Math.pow(base, expoente)));
}
exponeciacao(2, 3);